import Foundation

actor OnDeviceDJBrain: DJBrainProtocol {
    func generateTransition(meta: TransitionMetadata) async -> String {
        // Placeholder: Foundation Models integration deferred to Phase 2.
        return "Transitioning into the next vibe right now."
    }
}
