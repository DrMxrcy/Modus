name: Modus
targets:
  Modus:
    type: application
    platform: iOS
    deploymentTarget: "26.0"
    sources:
      - Modus
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.modus.app
      INFOPLIST_FILE: Modus/Resources/Info.plist
      SWIFT_VERSION: "6.0"
      SWIFT_STRICT_CONCURRENCY: complete
    info:
      path: Modus/Resources/Info.plist
      properties:
        UILaunchScreen: {}
